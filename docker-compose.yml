version: '3.8'

services:
  web:
    build: .
    volumes:
      - .:/app
    ports:
      - "8081:8081"
    env_file:
      - .env
    restart: unless-stopped
    mem_limit: 256m
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
